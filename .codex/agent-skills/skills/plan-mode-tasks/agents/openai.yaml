interface:
  display_name: "Plan Mode Tasks"
  short_description: "Create, validate, update progress, archive, and restore plan-mode task artifacts."
  default_prompt: "Use the registered plan_mode_tasks subagent and its agent-taskctl.py CLI to manage active and archived ./.agents/tasks/<task>/plan.md and tasks.md during Plan Mode."
